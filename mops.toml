[package]
name = "openclaw-icp"
version = "0.1.0"
description = "ICP-native AI agent platform with on-chain LLM, HTTPS outcalls, vetKD credentials, wallet, and agent identity"

[dependencies]
base = "0.16.0"
llm = "2.1.0"
ic = "3.2.0"
